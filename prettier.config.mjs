/** @type {import('prettier').Config} */
const config = {
    printWidth: 150,
    tabWidth: 4,
    useTabs: false,
    semi: true,
    singleQuote: true,
    trailingComma: 'es5',
    arrowParens: 'always',
};

export default config;
